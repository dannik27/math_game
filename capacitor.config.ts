import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dannik.math_game',
  appName: 'Умножение',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
